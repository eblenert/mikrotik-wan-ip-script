:global currentIP;
:global url;
:local newIP;
:local result;

:set newIP [/ip address get [find interface="rds"] address];

:if ($newIP != currentIP) do={
  :log info "IP address $currentIP changed to $newIP";
  :set currentIP $newIP;
  :set result [/tool fetch url="$url"];
}